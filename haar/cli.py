"""Command-line interface for Haar weather prediction system.

This module will be expanded in Issue #5 (CLI Framework).
"""

import sys


def main() -> int:
    """Main entry point for the haar CLI.

    Returns:
        Exit code (0 for success, non-zero for errors)
    """
    print("Haar Weather Prediction System v0.1.0")
    print("CLI framework coming in Issue #5...")
    return 0


if __name__ == "__main__":
    sys.exit(main())
